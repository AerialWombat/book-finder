import React from "react";
import SearchBar from "../SearchBar";

const Header = () => {
  return (
    <header>
      <h1>Book Finder</h1>
      <SearchBar />
    </header>
  );
};

export default Header;
